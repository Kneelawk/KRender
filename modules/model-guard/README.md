# Model Guard

The model guard system is used by model loaders to provide an opt-in interface for model formats. This prevents
KRender's model loaders from trampling other model loaders that load the same types of models.
